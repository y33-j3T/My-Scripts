sudo apt remove <package-name>  # remove app only
sudo apt purge <package-name>   # remove app & config
sudo apt clean       # clear package cache
sudo apt autoremove  # remove unrequired dependencies
sudo apt -f install  # fix broken install
